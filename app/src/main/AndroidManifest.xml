<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Не даёт CPU засыпать пока сервис считает -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- FIX: добавлено разрешение для точных будильников (AlarmManager.setExactAndAllowWhileIdle).
         Без него scheduleFinish() упадёт с SecurityException на Android 12+. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Timer">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:label="@string/app_name"
            android:theme="@style/Theme.Timer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FIX: exported="true" — AlarmManager отправляет broadcast из системного процесса,
             поэтому ресивер обязан быть экспортированным, иначе intent не доставится. -->
        <receiver
            android:name=".TimerReceiver"
            android:exported="true" />

        <service
            android:name=".TimerService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="timer" />
        </service>

    </application>

</manifest>